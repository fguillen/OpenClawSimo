# Secrets — ignore every .env* variant (.env, .env.production, .env.*.back, …)
# except the committed template.
.env
.env.*
!.env.example

# Runtime data
/data
# Live config lives in Dokploy's files dir; only openclaw.example.json is tracked
openclaw.json

# Editor cruft
.vscode/
.idea/
*.swp
.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db
